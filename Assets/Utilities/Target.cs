using BehaviorScripts;
using UnityEngine;

namespace Utilities
{
    //The following class represents a target from an IEntity objects perspective.
    /*
    public class Target
    {
        //If an entity has acquired a target and is within its ShooterBehaviors locking range, it has a lock.
        public bool targetLocked => lockedTarget != null && lockedTarget.IsAlive;
        //What is the position of the target relative to the owner of this object?
        private Vector2 m_TargetVector;
        public Vector2 targetVector
        {
            get => m_TargetVector;
            set
            {
                m_TargetVector = value;
                targetDistance = value.magnitude;
            }
        }
        //What is the target?
        public Entity lockedTarget { get; set; }
        //What is it's distance?
        public float targetDistance { get; set; }

    }
*/
}